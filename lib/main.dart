import 'package:adisyon/routes_default.dart';
import 'package:flutter/material.dart';

void main() => runApp(new MaterialApp(
      title: "Adisyon",
      debugShowCheckedModeBanner: false,
      initialRoute: '/',
      routes: routes,
    ));
